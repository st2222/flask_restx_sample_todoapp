version: 0.2
phases:
  install:
    runtime-versions:
        python: 3.8
  build:
    commands:
      - pip install --upgrade pip
      - pip install pipenv --user
      - mkdir -p ./lib/python/
      - pipenv install --dev --skip-lock --target ./lib/python/
      - pipenv install awscli aws-sam-cli --skip-lock
      - pipenv run sam package --template-file template.yaml --s3-bucket ${BUCKET_NAME} --output-template-file packaged-template.yml
artifacts:
  files:
    - packaged-template.yml

